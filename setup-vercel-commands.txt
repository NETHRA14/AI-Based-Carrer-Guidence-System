// Copy and run these commands one by one in your terminal:

vercel env add NEXT_PUBLIC_SUPABASE_URL production
// Enter when prompted: https://your_project.supabase.co

vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY production
// Enter when prompted: your_supabase_anon_key

vercel env add SUPABASE_SERVICE_ROLE_KEY production
// Enter when prompted: your_supabase_service_role_key

vercel env add DATABASE_URL production
// Enter when prompted: postgresql://postgres:your_password@db.your_project.supabase.co:5432/postgres

vercel env add JWT_SECRET production
// Enter when prompted: your_jwt_secret

vercel env add HUGGINGFACE_API_KEY production
// Enter when prompted: your_huggingface_api_key

vercel env add COHERE_API_KEY production
// Enter when prompted: your_cohere_api_key

vercel env add DEFAULT_AI_PROVIDER production
// Enter when prompted: huggingface

vercel env add NEXT_PUBLIC_GOOGLE_MAPS_API_KEY production
// Enter when prompted: your_google_maps_api_key

vercel env add NEXT_PUBLIC_APP_URL production
// Enter when prompted: https://your-actual-vercel-domain.vercel.app

// After setting all variables, redeploy:
vercel --prod